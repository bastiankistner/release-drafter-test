name: Lint PR title

on:
  pull_request:
    types: ['opened', 'edited', 'reopened', 'synchronize']

jobs:
  pr-lint:
    runs-on: ubuntu-latest
    steps:
    - uses: seferov/pr-lint-action@master
      with:
        title-regex: '^BR24-\d{1,4}\s'
        title-regex-flags: 'g' # optional
        error-message: 'Add Jira ID to your title' # optional